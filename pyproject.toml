[project]
name = "train-reservation"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flask",
    "requests>=2.33.1",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = [
    "train_data_service",
    "booking_reference_service",
]
